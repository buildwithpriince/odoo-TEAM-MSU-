# GlobeTrotter Backend + Frontend Integration

This package is intentionally **backend-only plus the minimal frontend integration layer** because the full frontend already exists in your GitHub repository.

## Structure

```text
globetrotter-backend-integration/
├── backend/
└── frontend-integration/
```

The frontend itself is NOT duplicated here.

See `backend/README.md` and `frontend-integration/README.md`.
